name: Deploy to Hugging Face Spaces

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
    - name: 🔄 Checkout Repository
      uses: actions/checkout@v3
      with:
        fetch-depth: 0

    - name: 🚀 Push to Hugging Face Spaces
      env:
        HF_TOKEN: ${{ secrets.HF_TOKEN }}
      run: |
        git config --global user.email "akshatkhandelwal004@gmail.com"
        git config --global user.name "akshat657"
        
        # Add Hugging Face Space as remote
        git remote add hf https://akshat657:$HF_TOKEN@huggingface.co/spaces/akshat657/ai-study-assistant || true
        
        # Force push to Hugging Face
        git push hf main --force

    - name: ✅ Deployment Complete
      run: |
        echo "🎉 Successfully deployed to Hugging Face Spaces!"
        echo "🔗 View your app at: https://huggingface.co/spaces/akshat657/ai-study-assistant"